linters:
  disable-all: true
  enable:
    # default
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - typecheck
    - unused
    # additional
    - asasalint
    - bodyclose
    - dupl
    - errname
    - errorlint
    - exhaustive
    - exportloopref
    - gocognit
    - goconst
    - gocritic
    - gocyclo
    - godox
    - goimports
    - gosec
    - lll
    - misspell
    - nolintlint
    - prealloc
    - revive
    - stylecheck
    - unconvert
    - unparam
    - whitespace

linters-settings:
  gocritic:
    disabled-checks:
      - ifElseChain
  whitespace:
    multi-if: true
    multi-func: true
